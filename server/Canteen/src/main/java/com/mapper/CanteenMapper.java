package com.mapper;

import java.util.List;

import org.apache.ibatis.annotations.*;

import com.fasterxml.jackson.databind.deser.std.NumberDeserializers.BooleanDeserializer;

//import com.entity.Users;
//import com.sun.istack.internal.FinalArrayList;

public interface CanteenMapper {

	// SELECT ALL / DISPLAY ALL

//	final String getUsers = "select * from users";
//
//
//	@Select(getUsers)
//	@Results(value = { @Result(property = "user_id", column = "user_id"),
//			@Result(property = "user_name", column = "user_name"), @Result(property = "password", column = "password"),
//			@Result(property = "email_id", column = "email_id"), @Result(property = "role", column = "role"), })
//	List<Users> getUsers();

	
}
